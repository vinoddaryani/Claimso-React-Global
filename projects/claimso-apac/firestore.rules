rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all reads and writes to all documents by default.
    // Specific access will be granted for authenticated users in subcollections/documents as needed.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}